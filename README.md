# 🚀 0xNote

> **轻量级 Markdown 编辑器 | Windows 右键菜单集成 | 实时预览 | 专业代码高亮**
>
> _Write specifically for Windows, but Structure for HarmonyOS._

<p align="center">
  <img src="https://img.shields.io/badge/Vue-3.5-4FC08D?style=flat-square&logo=vue.js&logoColor=white" alt="Vue 3" />
  <img src="https://img.shields.io/badge/TypeScript-5.8-3178C6?style=flat-square&logo=typescript&logoColor=white" alt="TypeScript" />
  <img src="https://img.shields.io/badge/Electron-35-47848F?style=flat-square&logo=electron&logoColor=white" alt="Electron" />
  <img src="https://img.shields.io/badge/Vite-7.0-646CFF?style=flat-square&logo=vite&logoColor=white" alt="Vite" />
  <img src="https://img.shields.io/badge/CodeMirror-6-D23B2E?style=flat-square" alt="CodeMirror" />
  <img src="https://img.shields.io/badge/License-AGPL_3.0-purple?style=flat-square" alt="License" />
</p>

---

## 📖 简介

**0xNote** 是一款专为开发者设计的极简 Markdown 编辑器。它不仅拥有现代化的 UI 和流畅的编写体验，更深层地探索了**跨平台架构**的最佳实践。

项目的核心目标是构建一个 **"一次编写，多端运行"** 的架构范本：

- ✅ **Phase 1 已完成**：Windows 客户端（Electron）
- 🚧 **Phase 2 规划中**：鸿蒙客户端（HarmonyOS Next / ArkTS）

---

## ✨ 核心特性

| 功能                  | 描述                                               |
| --------------------- | -------------------------------------------------- |
| 🎨 **专业级代码高亮** | 基于 CodeMirror 6，支持 100+ 编程语言语法高亮      |
| 👁️ **实时预览**       | 双栏分屏布局，左侧编写右侧实时预览，所见即所得     |
| ⚡ **极速启动**       | 毫秒级冷启动，拒绝臃肿，专注效率                   |
| 💾 **智能自动保存**   | 防抖保存机制，编辑即保存，永不丢失                 |
| �️ **右键菜单集成**    | 文件夹空白处右键「新建 Markdown 笔记」             |
| 📄 **文件关联**       | 双击 `.md` 文件直接用 0xNote 打开                  |
| ⚙️ **灵活配置**       | 字体、字号、主题、Tab 大小、自动保存延迟均可自定义 |
| 🌙 **护眼主题**       | 内置 Catppuccin 配色（暗色/亮色/跟随系统）         |
| 🔒 **XSS 防护**       | 预览使用 DOMPurify 净化 HTML，安全可靠             |

---

## 📸 功能演示

### 🖼️ 视图模式

标题栏右上角提供三种视图模式切换：

| 模式     | 图标 | 场景               |
| -------- | ---- | ------------------ |
| 分栏模式 | ⬚    | 边写边看，实时预览 |
| 仅编辑   | ✏️   | 专注写作，沉浸体验 |
| 仅预览   | 👁️   | 文档阅读，检查排版 |

### ⚙️ 设置面板

点击标题栏 ⚙️ 图标打开设置：

| 分类       | 选项                                   |
| ---------- | -------------------------------------- |
| **外观**   | 主题切换、字号调节 (12-24px)、字体选择 |
| **编辑器** | 显示行号、Tab 宽度 (2/4/8)             |
| **保存**   | 自动保存开关、保存延迟 (0.5-5秒)       |
| **预览**   | 同步滚动开关                           |

---

## 🛠️ 开发指南

### 环境要求

| 依赖    | 版本  |
| ------- | ----- |
| Node.js | >= 20 |
| npm     | >= 9  |

### 克隆与启动

```bash
# 克隆项目
git clone https://github.com/your-repo/0xNote.git
```

```bash
# 进入项目目录
cd 0xNote
```

```bash
# 安装依赖
npm install
```

```bash
# 启动 Electron 开发模式（推荐）
npm run electron:dev
```

### 可用脚本

| 命令                   | 描述                            |
| ---------------------- | ------------------------------- |
| `npm run dev`          | 启动 Vite 开发服务器 (Web)      |
| `npm run electron:dev` | 启动 Electron 开发模式          |
| `npm run build`        | 构建生产版本                    |
| `npm run build:win`    | 打包 Windows 安装程序 (.exe)    |
| `npm run build:mac`    | 打包 macOS 安装程序 (.dmg)      |
| `npm run build:linux`  | 打包 Linux 安装程序 (.AppImage) |
| `npm run type-check`   | TypeScript 类型检查             |
| `npm run lint`         | ESLint 代码检查                 |

构建产物输出到 `release/` 目录。

---

## 🏗️ 架构设计

本项目采用**「逻辑与视图分离」**的严格架构，确保业务逻辑与平台实现完全解耦。

### 核心原则

```
┌─────────────────────────────────────────────────────────┐
│                     Vue Components                      │
│         (MemoEditor, MemoPreview, Settings...)          │
└───────────────────────────┬─────────────────────────────┘
                            │ 调用
                            ▼
┌─────────────────────────────────────────────────────────┐
│                    Pinia Stores                         │
│         (fileStore, appStore, settingStore)             │
└───────────────────────────┬─────────────────────────────┘
                            │ 依赖接口
                            ▼
┌─────────────────────────────────────────────────────────┐
│               Platform Adapter                           │
│          (运行时检测平台，返回对应实现)                     │
└───────────────────────────┬─────────────────────────────┘
                            │
          ┌─────────────────┼─────────────────┐
          ▼                 ▼                 ▼
    ┌───────────┐     ┌───────────┐     ┌───────────┐
    │  Windows  │     │  Harmony  │     │   Web     │
    │ Electron  │     │  ArkTS    │     │  Browser  │
    └───────────┘     └───────────┘     └───────────┘
```

### 抽象接口

| 接口             | 职责     | Windows 实现 | 鸿蒙实现    |
| ---------------- | -------- | ------------ | ----------- |
| `IFileSystem`    | 文件读写 | Node.js fs   | fileIO      |
| `IConfigService` | 配置存储 | localStorage | Preferences |
| `ISystemMenu`    | 系统菜单 | 注册表操作   | N/A         |

### 鸿蒙迁移准备

| 层次       | 规范                                    |
| ---------- | --------------------------------------- |
| **UI 层**  | 禁止直接操作 DOM，样式优先 Flexbox      |
| **逻辑层** | 全 TypeScript，无隐式 `any`，易转 ArkTS |
| **结构层** | `src/platforms/harmony/` 已预留占位实现 |

---

## 📁 项目结构

```
0xNote/
├── src/
│   ├── common/types/          # 🌐 接口定义 (IFileSystem, IConfigService...)
│   ├── platforms/
│   │   ├── adapter.ts         # 🔀 平台适配器
│   │   ├── windows/           # 🪟 Windows 实现
│   │   └── harmony/           # 🔷 鸿蒙实现 (占位)
│   ├── components/            # 🧩 Vue 组件
│   │   ├── MemoEditor.vue     # 编辑器
│   │   ├── MemoPreview.vue    # 预览
│   │   └── SettingsModal.vue  # 设置
│   ├── stores/                # 🗃️ Pinia 状态
│   │   ├── fileStore.ts       # 文件管理
│   │   ├── appStore.ts        # 应用状态
│   │   └── settingStore.ts    # 设置管理
│   └── App.vue                # 主组件
│
├── electron/
│   ├── main.ts                # 🖥️ 主进程
│   ├── preload.ts             # 预加载脚本
│   └── registry.ts            # 注册表操作
│
├── build/
│   └── installer.nsh          # NSIS 安装脚本
│
├── docs/
│   ├── RELEASE_CHECKLIST.md   # 发布清单
│   ├── TEST_REPORT.md         # 测试报告
│   └── UI_TEST_GUIDE.md       # UI 测试指南
│
└── release/                   # 构建产物
```

---

## 🛡️ 技术栈

| 分类          | 技术                    | 版本       |
| ------------- | ----------------------- | ---------- |
| 前端框架      | Vue 3 (Composition API) | 3.5+       |
| 类型系统      | TypeScript              | 5.8+       |
| 构建工具      | Vite                    | 7.0+       |
| 状态管理      | Pinia                   | 3.0+       |
| 代码编辑器    | CodeMirror 6            | 6.0+       |
| Markdown 渲染 | markdown-it + DOMPurify | 14.x / 3.x |
| 桌面框架      | Electron                | 35+        |
| 安装打包      | electron-builder + NSIS | 26+        |

---

## 📄 开源协议

本项目采用 **AGPL-3.0** 协议开源。
